
{
  "name": "gui-e2e",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.45.0"
  },
  "scripts": {
    "test": "playwright test",
    "install:browsers": "npx playwright install --with-deps"
  }
}
