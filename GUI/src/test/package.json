
{
  "name": "gui-e2e",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "@playwright/test": "1.57.0",
    "http-server": "^14.1.1",
    "wait-on": "^7.2.0"
  },
  "scripts": {
    "install:browsers": "npx playwright install --with-deps",
    "serve": "http-server ../main/java/frontend -p 3000 --cors --silent",
    "test": "playwright test"
  }
}
